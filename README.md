# Prediction Service

This repository includes an Azure Function App, which is a service that periodically makes and saves new predictions using an API.

The `src/functions` directory has the function. The `src/services` directory has services for contaction the database and API.

## Dev Container

There is a Dev Container available, that can be used to setup the development environment.
